spring:
    application:
        name: web

system:
  log:
    enabled: true

web:
  interceptors:
    SimpleAuthInterceptor:
      exclude:
      - /auth/login
      - /static/*
      - /images/*
    AuthInterceptor:
      exclude:
      - /auth/login
      - /static/*
      - /images/*
