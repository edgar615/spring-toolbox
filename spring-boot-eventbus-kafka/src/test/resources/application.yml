eventbus:
    consumer:
        group: test
        servers:  localhost:9092
        topics:
          - DeviceControlEvent_1_3
    producer:
        retries: 1
        servers:  localhost:9092 #多个用逗号,分隔