spring:
    application:
        name: web
web:
  logConfig:
    showTrace: false
    showErrorStackTrace: false
  interceptors:
    SimpleAuthInterceptor:
      exclude:
      - /auth/login
      - /static/*
      - /images/*
    AuthInterceptor:
      exclude:
      - /auth/login
      - /static/*
      - /images/*
