spring:
    application:
        name: web
web:
  log:
    showTrace: false
  interceptors:
    SimpleAuthInterceptor:
      exclude:
      - /auth/login
      - /static/*
      - /images/*
    AuthInterceptor:
      exclude:
      - /auth/login
      - /static/*
      - /images/*
